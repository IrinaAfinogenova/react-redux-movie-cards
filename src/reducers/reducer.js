const initialState = {
    visibilityFilter: [],
    todos: []
};

const todoApp = (state) => {
    if (typeof state === 'undefined') {
        return initialState;
    }


    return state;
};

export default todoApp;